@using BlazorSpa.Client.Shared.Components
@inherits MainLayoutComponent

@if (AuthStore.IsAuth)
{
    <div class="sidebar">
        <NavMenu />
    </div>
}

<div class="main">
    @if (AuthStore.IsAuth)
    {
        <div class="top-row px-4">
            <NavLink class="ml-md-auto" href="chat"><span class="oi oi-bell"></span> <span class="badge badge-light">@Hub.UnreadMessages</span></NavLink>
            <NavLink class="ml-md-auto" href="logout"><span class="oi oi-account-logout"></span> Logout</NavLink>
        </div>
    }
    else
    {
        <div class="top-row px-4">
            <NavLink class="ml-md-auto" href="registration"><span class="oi oi-people"></span> Registration</NavLink>
        </div>
    }

    <Provide Value="theme">
        <div class="content px-4 ">
            @Body
        </div>
    </Provide>
</div>
